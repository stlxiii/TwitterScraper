Requirements:
    1. Python 3.x

    2. Install TinyDB using command:
       pip install tinydb

    3. Install Selenium using command:
       pip install selenium

    4. install Firefox and the Firefox webdriver
       https://github.com/mozilla/geckodriver/releases


Notes
   Tested only on Mac but should work on other platforms as well.


How to use:
    1. Enter URLs of tweets in config.ini
    
    2. Run download.py
       It may be necessary to run it several times until no
       new tweets are found.

    3. Run export.py
       Tweet information will be saved under /export/{tweet id}.csv